spring:
  application:
    name: studyflow-backend

  datasource:
    url: jdbc:postgresql://aws-1-ca-central-1.pooler.supabase.com:5432/postgres
    username: postgres.mmaxvihguvjschuwuhhu
    password: StudyApp2025+
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  mongodb:
    connection-string: ${MONGODB_URI:mongodb://localhost:27017/studyflow}
    database: studyflow

supabase:
  url: https://mmaxvihguvjschuwuhhu.supabase.co
  secret-key: ${SUPABASE_SECRET_KEY:}
